name: Tag release

on:
  push:
    branches: ["chore/tag-v0.11.2"]

permissions:
  contents: write

jobs:
  tag:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/github-script@v7
        with:
          script: |
            const tag = "v0.11.2";
            const sha = "5a39de98c11d2a96dc875b39b8178dec6c88f318"; // merge commit on main
            try {
              await github.rest.git.createRef({
                owner: context.repo.owner,
                repo: context.repo.repo,
                ref: `refs/tags/${tag}`,
                sha,
              });
              core.notice(`Created tag ${tag} -> ${sha}`);
            } catch (err) {
              if (`${err}`.includes("Reference already exists")) {
                core.notice(`Tag ${tag} already exists`);
              } else {
                throw err;
              }
            }
